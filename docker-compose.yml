services:
  challenge-webmcp:
    build: .
    image: challenge-webmcp:latest
    container_name: challenge-webmcp-ssr
    restart: unless-stopped
    ports:
      - "127.0.0.1:50016:4000"
    environment:
      - NODE_ENV=production
      - PORT=4000